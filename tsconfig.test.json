{
  "compilerOptions": {
    "module": "commonjs",
    "outDir": "./test-jasmine",
    /* Make sure proper types are used */
    "types": ["jasmine", "node", "webpack-env"],
    "sourceMap": true
  },
  "extends": "./tsconfig",
  "include": ["src", "test"],
  /* Exclude files that are Jest-only (not run under karma) */
  "exclude": ["test/_setupFiles/jest", "test/gsheets-compat"]
}
